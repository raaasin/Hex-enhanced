---
"hex-app": patch
---

Add configurable AI formatter provider settings (xAI/OpenAI/Gemini), remove temporary formatter placeholder insertion, and move the transcription indicator overlay to the bottom of the screen while preserving formatter fallback/error behavior (#0).
