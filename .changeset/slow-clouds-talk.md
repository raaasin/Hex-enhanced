---
"hex-app": patch
---

Add a selection-aware formatting path in transcription that captures highlighted text on record start and falls back to normal dictation when no valid selection is available.
